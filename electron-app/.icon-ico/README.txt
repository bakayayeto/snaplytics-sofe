Windows packaging uses the ICO here (see package.json build.win.icon).
Put your multi-size Windows icon at:

  icon.ico

If your file has another name, rename it to icon.ico or change build.win.icon in package.json.
